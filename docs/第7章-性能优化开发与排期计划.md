# OrangeASR 第7章 性能瓶颈与数据链路优化 — 开发与排期计划

> **对应 `todo_list.md §7`** | 版本 v1.0 | 2026-09-09 | 负责人：待指派 | 排期：6 周（2026-09-15 ~ 2026-10-26）

---

## 1. 背景与目标

### 1.1 现状痛点

前端 Gradio → Gateway WebSocket → NATS JetStream → ASR Worker (faster-whisper) → NATS → Gateway/Storage/Refiner 全链路存在 6 类瓶颈：同步阻塞、Base64 膨胀 33%、固定 2s 硬切、单线程推理、同步落盘、LLM 同步调用，导致端到端延迟 2.5~3.5s、GPU 利用率 <20%、单点不可扩展。

### 1.2 优化目标（量化）

| 指标 | 现状 | P0 完成后 | P0+P1 完成后 | 测量方式 |
|------|------|-----------|--------------|----------|
| 端到端延迟 | 2.5~3.5s | 1.0~1.5s (-55%) | 0.8~1.2s (-65%) | `client_capture_ts` → 前端显示时间戳 |
| NATS 单消息带宽 | 85KB | 55KB (-35%) | 15KB (-82%, Opus) | `nats --jsz` + Prometheus `nats_bytes` |
| ASR Worker 吞吐 | 0.5 req/s/GPU | 1.5 req/s | 3.0 req/s | `asr_worker_inference_seconds` histogram |
| Storage 吞吐 | 2 msg/s | 10 msg/s | 20 msg/s | `storage_s3_upload_seconds` |
| 并发能力 | 1 Gateway 单点 | 2 副本 + QueueGroup | HPA 2~5 副本 | 10 并发客户端压测 |

### 1.3 约束

* 硬件：1× Jetson Orin Nano (sm_87, 8GB) + 1× Raspberry Pi 4B
* 兼容性：优化期间保持对外 WebSocket 协议兼容，NATS 需兼容 Base64 老消息（灰度期双读）
* 不影响 §1-§6 排期，独立分支 `perf/pipeline-opt`

---

## 2. 总体排期总览

```
W0 (09/09)        ✅ 阶段0 90% 提前冻结（原 W1）
W1 (09/15-09/21)  真机复测窗口（F1-F4/G1-G2/H1-H4 延伸）
W2-W3 (09/22-10/05) ✅ P0 92% 提前完成（09-09 代码就绪）
W4 (10/06-10/12)  ✅ P1 95% 提前完成（09-09 代码就绪）
W5 (10/13-10/19)  ✅ P2 96% 提前完成（09-09 代码就绪，原 7 人日释放）
W6 (10/20-10/26)  联调、灰度、文档与交付 — 可提前至 09/10-09/25
缓冲 1 周 (10/27-11/02) 风险预留
```

| 阶段 | 里程碑 | 交付物 | 准入条件 | 准出标准 | 实际(09-09) |
|------|--------|--------|----------|----------|-----------|
| M0 | 基线冻结 | `benchmark_e2e.py` + 基线报告 | 代码冻结 | 10 并发下延迟/吞吐基线可复现 | ✅ 已冻结（mock 基线 p50 35~42ms，真实推算 p50 2.6s）；待真机 10 并发复测 |
| M1 | P0 完成 | Gateway VAD + 二进制 NATS + 量化微批 | M0 通过 | 延迟 <1.5s, 带宽 -30% | ✅ 92% 代码完成（09-09），mock 验证通过；待真机 WER/P95 |
| M2 | P1 完成 | 异步化三 Worker + NATS 统一定流 | M1 通过 | 吞吐 3× 提升, 无阻塞告警 | ✅ 95% 代码完成（09-09），18 passed；待吞吐实测 |
| M3 | P2 完成 | Prometheus + HPA + 背压协议 | M2 通过 | Grafana 看板 + HPA 自动扩容验证 | ✅ 96% 代码完成（09-09），25 passed；待 HPA 集群压测 |
| M4 | 发布 | 灰度 100% + 运维手册 | M3 通过 | 7 天无 P1 缺陷 | ⏳ 未开始，提前至 09/16 启动 |

---

## 3. 任务分解（WBS）

### 阶段 0：基准与脚手架（W1，5人日）— ✅ 已完成 90%（2026-09-09 提前冻结，计划 09/15-09/21）

| 编号 | 任务 | 负责人 | 工期 | 依赖 | 产出文件 | 状态(09-09) | 备注/待补 |
|------|------|--------|------|------|----------|-------------|-----------|
| 0.1 | 编写 E2E 基准脚本 `tests/benchmark_e2e.py`：模拟 1/5/10 并发客户端，注入 `capture_ts`，统计 P50/P95 延迟、吞吐、NATS pending | 后端 A | 1.5d | - | `tests/benchmark_e2e.py` | ✅ 95% | 并发 1/5/10 CLI 已支持，pytest 固定 1,5 需补 10；mock 降级可用，真实 `jsz pending` 待真机复测 |
| 0.2 | 压测脚本 `tests/load_nats_flood.py`：100 msg/s 洪峰，断言 `max_payload` 与 pending <100 | 后端 A | 0.5d | 0.1 | 同上 | ✅ 100% | CI 已过 `test_nats_flood_100rps`，真实 JetStream 待 P1 统一定流后复测 |
| 0.3 | `py-spy` / `nats monitoring /jsz` 基线采集，输出火焰图与 `baseline_report.md` | 后端 B | 1d | 0.1 | `docs/baseline_report.md` | ⚠️ 80% | `baseline_report.md` 已冻结（含链路分解+瓶颈归因），但离线环境：火焰图/`/jsz` 为静态推算，待 Jetson 真机 + NATS 补采 |
| 0.4 | 创建特性分支与 CI：`perf/pipeline-opt`，增加 `pytest -m benchmark` 门禁 | DevOps | 1d | - | `.github/workflows/perf.yml` | ✅ 90% | 分支实为 `perf/pipeline`（与计划命名不一致，建议 `git branch -m` 或保留别名）；`perf.yml` p95<1500ms 门禁已生效，需补 `pytest.ini` 注册 `benchmark` mark 消 warning |
| 0.5 | 定义 Stream 规范草案：`ASR_INPUT`/`ASR_OUTPUT` 保留、TTL、副本 | 架构 | 1d | - | `deploy/k8s/nats-init.yaml` 草案 | ✅ 95% | `nats-init.yaml` 已交付（limits/5m/512MB/file/old），`replicas=1` 未预留 HA，Worker `add_stream` 保留待 P1 移除正确 |

> **阶段 0 小结（09-09 评估）：** 5 项完成 4.5 项，整体 90%，基准可复现（`pytest -m benchmark` 2 passed，`benchmark_result.json` 已生成）。提前 6 天完成，具备进入 P0 准入条件；3 项待补列入 W2 前置修复，不阻塞关键路径。

### 阶段 1：P0 关键路径（W2-W3，12人日）— ✅ 已完成 92%（2026-09-09，提前 13 天）

#### 1A Gateway 滑动窗口 + VAD（W2，4人日）— ✅ 100%

| 编号 | 任务 | 工期 | 技术要点 | 验收 | 状态(09-09) |
|------|------|------|----------|------|-------------|
| 1A.1 | 替换固定 `THRESHOLD_BYTES=2s` 为自适应：0.8~1.5s + `webrtcvad` (aggressiveness=2) 或复用 `faster-whisper VAD` | 2d | `src/gateway.py:119` 增加 `VADChunker` 类，`min_silence=300ms`，保留 0.3s overlap 而非 `clear()` | 单句切分正确率 >95%，WER 不劣化 | ✅ 已交付 `VADChunker`（webrtcvad 优先 + 能量回退），支持 0.8/1.5/0.3 可配 |
| 1A.2 | 背压与 Ack：`await js.publish(..., timeout=2)` 校验 ack，TokenBucket 10 msg/s/session，`max_pending=20` 丢弃最老并下发 `{"type":"busy"}` | 1d | `asyncio.Semaphore` + `collections.deque` | 洪峰下无 NATS OOM | ✅ 已交付 `TokenBucket` + `ack timeout 2s` + `busy/backpressure` 消息 |
| 1A.3 | 单测 `test_gateway_chunker.py`：静音/截断/重叠场景 | 1d | pytest | 覆盖率 >80% | ✅ 已交付 8 用例全过，覆盖静音/截断/重叠/限流 |

#### 1B 去 Base64 二进制化（W2，3人日）— ✅ 85%

| 编号 | 任务 | 工期 | 技术要点 | 验收 | 状态(09-09) |
|------|------|------|----------|------|-------------|
| 1B.1 | NATS 消息改为 `headers={"content-type":"audio/pcm"}` + `data=pcm_bytes`，或 `msgpack` 封装 `{meta: json, audio: bytes}`；保留 `audio_b64` 兼容读 | 1.5d | 涉及 `gateway.py:131`, `asr_worker.py:79`, `storage_worker.py:92` | 带宽 -33%，CPU -5ms/hop | ✅ 已交付 三端双读（headers+data 与 audio_b64 自适应） |
| 1B.2 | 灰度开关 `USE_BINARY_PAYLOAD=1`，双写双读 1 周 | 0.5d | `Config.USE_BINARY` | 老客户端仍可用 | ✅ 已交付 `Config.USE_BINARY_PAYLOAD` + `asr-config` 开关，回退 `USE_BINARY=0` |
| 1B.3 | 更新 `deploy/k8s/01-infra.yaml` `max_payload` 保持 10MB，增加 `max_msg_size` 监控告警 | 1d | - | 监控无 `payload exceeded` | ⚠️ 60% `max_payload 10MB` 未改动正确，但监控告警未加（待 P2 Prometheus 时补） |

#### 1C ASR Worker 量化 + 微批（W3，5人日）— ✅ 90%

| 编号 | 任务 | 工期 | 技术要点 | 验收 | 状态(09-09) |
|------|------|------|----------|------|-------------|
| 1C.1 | `compute_type=float16 → int8_float16`，`config.py:14` 与 `02-apps.yaml` 同步；在 Orin Nano 验证精度（tiny 模型 WER 波动 <1%） | 1d | `WhisperModel("tiny", compute_type="int8_float16")` | 显存 -40%，延迟 -30% | ✅ 代码已切 `int8_float16`（`config.py`+`02-apps.yaml`），⚠️ 真机 WER 待 `test_transcribe.py` 对比 |
| 1C.2 | 微批队列：50~100ms 窗口聚合，`asyncio.Queue` + `Semaphore(max_concurrency=2)`，批量 `run_in_executor` 并发 | 2d | `src/asr_worker.py:85` 重构为 `BatchProcessor` | 吞吐 0.5→1.5 req/s | ✅ 已交付 `batch_loop` 80ms 窗口 + `Semaphore(2)` + `batch≤4` |
| 1C.3 | 模型 Warmup：启动时 1s 静音 dummy 推理 + KV cache 预热 | 0.5d | `load_model()` 后 `model.transcribe(np.zeros(16000))` | 首包延迟 <300ms | ✅ 已交付 `ASR_WARMUP=1` 时 1s 零音 warmup |
| 1C.4 | 超时与异常隔离：单条推理 `wait_for 5s`，超时 `ack` 并 `nak` 重试 1 次 | 0.5d | `asyncio.wait_for` | 无单条阻塞全 Worker | ✅ 已交付 `wait_for 5s` 隔离，超时不阻塞 |
| 1C.5 | 压测验证 `benchmark_e2e` P95 <1.5s | 1d | - | 达标 | ⚠️ 70% mock P95 55ms 已达标，真机 P95 <1.5s 待 Jetson 实测 |

> **阶段 1 小结（09-09）：** 11/12 人日完成，整体 92%，11 项测试全过（`test_gateway_chunker` 8 + `benchmark` 2 + `flood` 1），代码已具备灰度条件；剩余 8% 为真机精度/性能复测与 01-infra 监控告警，不阻塞进入 P1。

### 阶段 2：P1 吞吐与资源效率（W4，10人日）— ✅ 已完成 95%（2026-09-09，提前 27 天）

| 编号 | 任务 | 负责人 | 工期 | 技术要点 | 文件 | 状态(09-09) |
|------|------|--------|------|----------|------|-------------|
| 2.1 | **WebUI 异步化**：`websocket-client` → `websockets` 异步或独立发送线程；缓存 `resample_poly` FIR 系数（按 `sr` 维度的 `dict`）；收发解耦（独立 `recv` 线程 + `gr.update` 回调） | 前端 | 3d | `src/web_ui.py:60,83,147` | `src/web_ui.py` | ✅ 95% `_resample_cache`+`send_queue(20)`+`_send_loop`+`backpressure`+`poll_updates` 已交付；待 Gradio 真机联调 |
| 2.2 | **Storage 异步化**：`boto3`→`aioboto3`，`pymongo`→`motor`；若保留同步则包 `run_in_executor`；批量 `insert_many` 缓冲 10条/1s；复用 `BytesIO` 池 | 后端 B | 2.5d | `src/storage_worker.py:102,126` | `src/storage_worker.py` | ✅ 95% `aioboto3/motor` 优先+`run_in_executor`兜底+`_buffer 10/1s`+`BytesIO`池已交付；待 MinIO/Mongo 吞吐实测 |
| 2.3 | **Refiner 异步化**：`requests`→`httpx.AsyncClient`，指数退避重试 2 次，熔断 3 次后 30s 冷却；修复 `call_later` 泄漏（`pop` 时 `cancel()` + `try/except RuntimeError`） | 后端 B | 2d | `src/refiner.py:56`, `refiner_worker.py:85` | `src/refiner*.py` | ✅ 100% `httpx AsyncClient`+`arefine`+`circuit breaker`+`call_later` 修复已交付，`motor` 异步落库 |
| 2.4 | **NATS 统一定流**：新增 `deploy/k8s/nats-init.yaml` Job 声明两 Stream（`retention=limits, max_age=5m, max_bytes=512MB, storage=file`）；Worker 移除 `add_stream`，仅 `subscribe(durable)`；拆分 `asr.output.transcript` (轻) 与 `asr.output.archive` (含音频) 或用 headers 过滤 | DevOps | 2.5d | `asr_worker.py:124` 等三处 | `deploy/k8s/nats-init.yaml` | ✅ 100% 三 Worker 已移除 `add_stream`，`asr_worker` 双发 `transcript/archive`，`gateway/storage/refiner` 双订阅兼容 |

> **阶段 2 小结（09-09）：** 9.5/10 人日完成，整体 95%，`tests/test_stage2.py` 7 + `test_gateway_chunker` 8 + `benchmark` 3 = 18 passed；代码级 P1 目标已达成，剩余 5% 为真机联调与吞吐压测，不阻塞进入 P2。

### 阶段 3：P2 可观测与弹性（W5，7人日）— ✅ 已完成 96%（2026-09-09，提前 34 天）

| 编号 | 任务 | 工期 | 技术要点 | 验收 | 状态(09-09) |
|------|------|------|----------|------|-------------|
| 3.1 | Prometheus 指标：`gateway_ws_connections`, `nats_publish_latency`, `worker_inference_seconds` (histogram), `worker_queue_depth`, `s3_upload_seconds`, `llm_latency`；Gateway `/metrics` + Worker sidecar HTTP `/healthz` | 2d | `prometheus_client` | Grafana 看板可查 | ✅ 100% `src/metrics.py` 9指标+`HAS_PROM`降级，Gateway `/metrics|/healthz|/ready`，Workers `8081/8082/8083` sidecar |
| 3.2 | 健康检查：Gateway `liveness/readiness` 探针校验 NATS 连通；Worker 心跳文件/HTTP 探针校验模型已加载 | 1.5d | `02-apps.yaml` 增加 `livenessProbe` | K8s 自动重启异常 Pod | ✅ 100% `02-apps.yaml` 全量 `liveness/readiness` + `resources requests/limits` + `metrics` 端口 |
| 3.3 | HPA：`asr-gateway` CPU 70% + 自定义 `nats_consumer_pending` (via `keda` 或 `prometheus-adapter`) 2~5 副本 | 1.5d | `deploy/k8s/hpa.yaml` | 压测自动扩容 | ✅ 100% `deploy/k8s/hpa.yaml` gateway 2-5 + worker 1-3 双 HPA，含 behavior 窗口 |
| 3.4 | 背压协议：Gateway→WebUI `{"type":"backpressure","pending":N}`，前端限流丢弃/合并 | 1d | `gateway.py` + `web_ui.py` | 前端 pending>20 时限速 | ✅ 100% `gateway_backpressure_total`+`TokenBucket`+WebUI `_backpressure_until` 已打通，P1 已验证 |
| 3.5 | 全链路压测调优：10 并发 × 5 分钟，记录 Grafana 与 `jsz`，产出 `perf_tuning_report.md` | 1d | - | P95 <1.2s | ✅ 90% `Grafana看板.json` 8面板+告警+`perf_tuning_report.md` 已交付，`P95<1.2s` 待真机 10并发×5min |

> **阶段 3 小结（09-09）：** 6.7/7 人日完成，整体 96%，`tests/test_stage3.py` 7 + 全量 25 passed；代码级 P2 目标已达成，剩余 4% 为集群 HPA 与全链路压测实测，不阻塞进入发布阶段。

### 阶段 4：联调与发布（W6，5人日）

| 编号 | 任务 | 工期 | 验收 |
|------|------|------|------|
| 4.1 | 集成测试：`tests/test_nats_core.py` + `benchmark_e2e` 全量回归 | 1d | CI 绿 |
| 4.2 | 灰度发布：二进制开关 10%→50%→100%，观察 3 天 | 1.5d | 无 P1 缺陷 |
| 4.3 | 文档：`README` 更新架构图与配置表，`docs/运维手册.md` | 1d | - |
| 4.4 | 复盘与 P3 预研：流式 ASR / gRPC / Opus 评估报告 | 1.5d | `docs/P3_流式预研.md` |

---

## 4. 人力与分工

| 角色 | 人数 | 职责 |
|------|------|------|
| 架构/后端 A | 1 | Gateway + ASR Worker + 基准脚本 |
| 后端 B | 1 | Storage/Refiner 异步化 + NATS 统一定流 |
| 前端 | 1 | WebUI 异步化 + 背压联动 |
| DevOps | 1 | K8s 清单、HPA、CI、灰度 |
| 测试 | 1 (兼) | 基准、压测、Grafana 看板 |

**总人日：39 人日 ≈ 6 周 × 1.5 人全职并行**，关键路径为 1A→1B→1C 串行，其余可并行。

---

## 5. 关键技术设计摘要

### 5.1 Gateway VAD 滑动窗口

```python
class VADChunker:
    def __init__(self, sample_rate=16000, min_chunk=0.8, max_chunk=1.5, overlap=0.3):
        self.vad = webrtcvad.Vad(2)
        self.buf = bytearray()
        self.overlap_bytes = int(overlap * sample_rate * 2)
    def feed(self, data: bytes) -> list[bytes]:
        self.buf.extend(data)
        if self._has_silence() or len(self.buf) >= max_chunk_bytes:
            chunk, self.buf = self.buf[:cut], self.buf[cut - self.overlap_bytes:]
            return [chunk]
        return []
```

### 5.2 二进制 NATS 负载

```python
# 发布
headers = {"req_id": req_id, "session_id": session_id, "ts": str(time.time())}
await js.publish("asr.input", pcm_bytes, headers=headers)
# 订阅
async def process_msg(msg):
    pcm = msg.data
    meta = msg.headers  # 无需 base64
```

灰度期兼容：`if "audio_b64" in data: pcm = b64decode(...) else: pcm = msg.data`

### 5.3 ASR 微批

```python
queue = asyncio.Queue()
sem = asyncio.Semaphore(2)
async def batch_loop():
    while True:
        batch = [await queue.get()]
        await asyncio.sleep(0.05)
        while not queue.empty() and len(batch) < 4:
            batch.append(queue.get_nowait())
        await asyncio.gather(*[infer_with_sem(b) for b in batch])
```

---

## 6. 依赖与风险

| 风险 | 概率 | 影响 | 缓解 |
|------|------|------|------|
| `int8_float16` 在 sm_87 精度回退 | 中 | 高 | W3 前用 `test_transcribe.py` 对比 WER，保留 `float16` 回滚开关 |
| `webrtcvad` 对中文/噪声误判 | 中 | 中 | 参数可调，提供 `VAD_DISABLE=1` 回退固定窗口 |
| `aioboto3`/`motor` 引入新依赖体积 | 低 | 中 | 先用 `run_in_executor` 兜底，异步化分两步 |
| NATS Stream 重建导致历史消息丢失 | 低 | 高 | 灰度前 `nats stream backup`，新 Stream 双写 1 天 |
| 前端 `websockets` 与 Gradio 兼容 | 中 | 中 | 备选方案：独立线程 + `queue.Queue` 发送队列 |

---

## 7. 验收与度量

* **门禁**：`pytest tests/benchmark_e2e.py --benchmark` P95 <1.5s (P0) / <1.2s (P1) 否则 CI 失败
* **监控**：Grafana 看板 `OrangeASR Perf` 包含 6 大金指标，告警 `pending>100`、`inference_p95>2s`
* **发布标准**：灰度 7 天无 P1 缺陷，回滚预案：一键 `kubectl rollout undo` + `USE_BINARY_PAYLOAD=0`

---

## 8. 交付清单

- [x] `tests/benchmark_e2e.py` + `baseline_report.md` — 阶段0 已冻结
- [x] `src/gateway.py` (VADChunker + 二进制 + 背压 + NATS 拆分订阅 + /metrics) — 阶段1-3 已交付
- [x] `src/asr_worker.py` (int8 + 微批 + warmup + transcript/archive + metrics) — 阶段1-3 已交付
- [x] `src/web_ui.py` (异步 WS + 缓存重采样) — 阶段2 已交付
- [x] `src/storage_worker.py` / `src/refiner*.py` (异步化 + metrics) — 阶段2-3 已交付
- [x] `deploy/k8s/nats-init.yaml` + `hpa.yaml` + 更新 `02-apps.yaml` — 全部已交付（含 probes + resources + HPA 2-5）
- [x] `docs/Grafana看板.json` + `docs/perf_tuning_report.md` — P2 已交付 8 面板 + 调优报告；待 `运维手册.md`/`P3_流式预研.md`
- [x] `src/metrics.py` — P2 新增
- [x] `tests/test_gateway_chunker.py` + `tests/test_stage2.py` + `tests/test_stage3.py` — 阶段1-3 共 22 用例

---

## 9. 附录：甘特图（文字版）

```
W1 ██████░░░░  基准 + 脚手架
W2 ██████████  Gateway VAD + 二进制
W3 ██████████  ASR 量化微批
W4 ██████████  WebUI/Storage/Refiner/NATS
W5 ██████░░░░  观测 + HPA + 压测
W6 ██████░░░░  联调灰度文档
```

> P3（流式 ASR / gRPC / Opus）不纳入本次排期，作为下季度预研，见 `docs/P3_流式预研.md` 占位。

---

## 10. 阶段 0 评估与计划更新（2026-09-09）

### 10.1 评估结论

* **进度：** 计划 W1（09/15-09/21）5 人日，实际 09-09 已完成 90%，提前 6 天；`perf/pipeline` 分支已推送，`pytest -m benchmark` 2/2 通过。
* **质量：** mock 基线可复现，但真实 Jetson + JetStream 基线尚未采集，标记为**离线基线**；`baseline_report.md` 结论与 `todo_list §7` 一致（E2E 2.6s，带宽 84KB/msg）。
* **准入判断：** 满足 M0 准出（代码冻结除外——新增文件未提交），建议补齐下述 3 项待补后即进入 P0。

### 10.2 待补清单（W1 前置修复，合计 0.5 人日，不阻塞关键路径）

| # | 待补 | 动作 | 负责人 | 截止 |
|---|------|------|--------|------|
| F1 | 真机基线复测 | Jetson Orin Nano 上 `pytest tests/benchmark_e2e.py --concurrency 1 5 10` + `nats --jsz` + `py-spy record -o flame.svg -- python -m src.asr_worker` 更新 `baseline_report.md` 附录 | 后端 B | 09/16 |
| F2 | CI mark 警告 | `pytest.ini` 或 `pyproject.toml` 增加 `markers = benchmark: perf gate` | DevOps | 09/15 |
| F3 | 分支命名对齐 | `git branch -m perf/pipeline perf/pipeline-opt` 并更新 `perf.yml` `branches: [perf/**]`，或在文档注明别名 | DevOps | 09/15 |
| F4 | 文件提交 | `git add tests/benchmark_e2e.py tests/load_nats_flood.py docs/baseline_report.md docs/benchmark_result.json deploy/k8s/nats-init.yaml .github/workflows/perf.yml` 并提交 | 后端 A | 09/15 |

### 10.3 排期调整

* **W1 职责变更：** 原“基础设施+基准”改为“真机复测+缺口修复”，释放 4 人日可提前启动 P0 设计（`VADChunker` 接口 + 二进制兼容方案评审）。
* **关键路径不变：** 1A→1B→1C 串行仍为关键路径，W2 按期开工；若 F1 阻塞，允许 P0 并行（mock 基线已足够支撑 VAD/二进制开发）。
* **缓冲：** 原缓冲 1 周保留至 10/27-11/02，应对 `int8_float16` 精度与 `webrtcvad` 误判风险。

### 10.4 下周（W1-W2）立即动作

1. 合并阶段 0 产物至 `main`（或保持 `perf/pipeline-opt`），打 tag `M0-baseline-2026-09-09`。
2. 召开 P0 技术评审：`webrtcvad` vs `faster-whisper VAD` 选型、`USE_BINARY_PAYLOAD` 灰度开关设计。
3. 启动 1B.1 二进制负载开发（可与 F1 并行）。

### 10.5 风险更新

* **新增：** Mock 基线与真实延迟偏差（mock 35ms vs 真实 2.6s）可能误导 P0 验收阈值 — 缓解：CI 增加注释 `mock 阈值 1500ms 仅防回归，真实门禁待真机 job`。
* **原风险保留：** `int8_float16`/`webrtcvad`/`aioboto3` 体积风险不变。

---

## 11. 阶段 1 评估与计划更新（2026-09-09）

### 11.1 评估结论（对标 W2-W3 计划 12 人日）

* **进度：** 计划 W2(09/22-09/28)+W3(09/29-10/05) 共 12 人日，实际 09-09 已完成代码 92%（约 11 人日），提前 13-20 天。`pytest` 11 passed（含 `test_gateway_chunker` 8），`src/gateway.py`+`src/asr_worker.py`+`src/config.py`+`deploy/k8s/02-apps.yaml` 已就绪，未提交。
* **质量：** 代码级 P0 目标已达成（二进制 -33%、VAD 自适应、量化 warmup、微批 80ms/Semaphore2），但两项真机验证缺口：① `int8_float16` 在 sm_87 的 WER 波动未实测 ② `benchmark_e2e` P95<1.5s 仅 mock 通过（55ms），需 Jetson + JetStream 实测；`01-infra.yaml` 监控告警未加。
* **准入判断：** 满足 M1 代码准出，可进入 P1；建议真机复测与 WER 对比作为 M1 关门项，不阻塞 P1 并行。

### 11.2 分项完成度

| 阶段 | 计划人日 | 完成度 | 已交付 | 待补 |
|------|----------|--------|--------|------|
| 1A Gateway VAD | 4 | ✅ 100% | `VADChunker`+`TokenBucket`+`test_gateway_chunker.py` 8/8 | 无 |
| 1B 二进制化 | 3 | ✅ 85% | 三端双读 + `USE_BINARY_PAYLOAD` 灰度开关 + `02-apps` 配置 | `01-infra` 监控告警（迁 P2 Prometheus） |
| 1C 量化微批 | 5 | ✅ 90% | `int8_float16`+`batch_loop`+`warmup`+`wait_for 5s` | 真机 `test_transcribe` WER<1% + `benchmark` P95 实测 |

### 11.3 待补清单（合计 1 人日，列为 M1 关门项，可与 P1 并行）

| # | 待补 | 动作 | 负责人 | 截止 |
|---|------|------|--------|------|
| G1 | 真机 WER 精度 | Orin Nano 上 `pytest tests/src/test_transcribe.py` 对比 `float16` vs `int8_float16`（`california.mp3` WER 波动 <1%），异常则回退 `ASR_COMPUTE_TYPE=float16` | 后端 A | 09/16 |
| G2 | 真机 P95 达标 | Jetson+NATS 真环境 `python tests/benchmark_e2e.py --concurrency 1 5 10 --requests 5` 验证 P95<1.5s，更新 `baseline_report.md` | 后端 B | 09/18 |
| G3 | 基础设施监控 | `01-infra.yaml` 保持 `max_payload 10MB`，监控告警并入 P2 `3.1 Prometheus`（`nats_payload_exceeded_total`） | DevOps | P2 统一 |
| G4 | 文件提交 | `git add src/gateway.py src/asr_worker.py src/config.py src/storage_worker.py deploy/k8s/02-apps.yaml deploy/requirements.txt tests/test_gateway_chunker.py` | 后端 A | 09/10 |

### 11.4 排期调整

* **总体提前：** 阶段1 提前约 2 周完成代码，原 W2-W3 释放为缓冲/真机验证窗口；P1（W4 10/06-10/12）可提前至 **09/10-09/22** 启动，关键路径仍为 1A→1B→1C 已打通，P1 四任务（2.1-2.4）可并行。
* **W3 职责变更：** 原“推理加速”改为“真机验证 + P1 预研（WebUI/Storage 异步化选型）”；若 G1 失败触发回滚，缓冲 1 周（10/27-11/02）兜底。
* **里程碑更新：** M1 标记为“代码完成 92%，待真机关门”，M2 提前；甘特图 W2/W3 合并为“P0 已完成”。
* **缓冲保留：** 原缓冲 1 周不变，应对 `int8_float16`/`webrtcvad` 误判回退。

### 11.5 下周（P1）立即动作

1. 提交阶段1 产物并打 tag `M1-p0-2026-09-09`，灰度开关 `USE_BINARY_PAYLOAD=1` 先内测 10%，观察 `busy/backpressure` 日志。
2. 启动 P1：2.1 WebUI 异步化、2.2 Storage `aioboto3/motor`、2.4 NATS 统一 Job（Worker 移除 `add_stream`）。
3. 预约 Jetson 真机窗口完成 G1/G2，产出 `docs/perf_stage1_report.md`。

### 11.6 风险更新

* **降低：** `webrtcvad` 误判风险已缓解（能量回退 + `VAD_DISABLE=1` 开关，`gateway.py` 已支持）。
* **新增：** `int8_float16` 未真机验证为当前最高风险 — 缓解：保留 `float16` 回滚一键 `kubectl set env deployment/asr-worker ASR_COMPUTE_TYPE=float16`。
* **原风险保留：** `aioboto3/motor` 体积、`webrtcvad` 依赖体积已通过可选安装缓解。

---

## 12. 阶段 2 评估与计划更新（2026-09-09）

### 12.1 评估结论（对标 W4 计划 10 人日）

* **进度：** 计划 W4(10/06-10/12) 10 人日，实际 09-09 已完成代码 95%（约 9.5 人日），提前 27 天。`pytest` 18 passed（`test_stage2` 7 + `test_gateway_chunker` 8 + `benchmark` 3），`src/web_ui.py`+`src/storage_worker.py`+`src/refiner*.py`+`nats-init` 均已就绪，未提交。
* **质量：** 代码级 P1 目标已达成（WebUI 缓存+队列化、Storage 批量+异步、Refiner 熔断+异步、NATS 统一拆分），剩余 5% 为真机联调与吞吐压测：WebUI Gradio 手动联调、Storage `insert_many` 20 msg/s、Refiner Ollama 真连、NATS 双流切换验证。
* **准入判断：** 满足 M2 代码准出，可进入 P2；真机吞吐验证作为 M2 关门项，不阻塞 P2 并行。

### 12.2 分项完成度

| 阶段 | 计划人日 | 完成度 | 已交付 | 待补 |
|------|----------|--------|--------|------|
| 2.1 WebUI | 3 | ✅ 95% | `resample_cache`+`send_queue`+`backpressure`+`poll_updates` | Gradio 真机联调 |
| 2.2 Storage | 2.5 | ✅ 95% | `aioboto3/motor`+`Batch 10/1s`+`BytesIO`池+`archive`订阅 | MinIO/Mongo 吞吐 20 msg/s 实测 |
| 2.3 Refiner | 2 | ✅ 100% | `httpx`+`retry`+`circuit breaker`+`timer` 修复+`motor` | Ollama 真连 30s 超时验证 |
| 2.4 NATS | 2.5 | ✅ 100% | `nats-init`双流+`add_stream`移除+`transcript/archive`拆分 | 集群 `nats stream ls/info` 验证 |

### 12.3 待补清单（合计 1.5 人日，M2 关门项，可与 P2 并行）

| # | 待补 | 动作 | 负责人 | 截止 |
|---|------|------|--------|------|
| H1 | WebUI 联调 | 本地 `python -m src.web_ui` + Gateway 真连，验证 resample 缓存与 backpressure 文案显示 | 前端 | 09/16 |
| H2 | 吞吐压测 | Jetson 上 `tests/load_nats_flood.py --rps 100` + `benchmark_e2e --concurrency 10` 验证 Storage 20 msg/s、ASR 1.5 req/s、P95<1.2s | 后端 B | 09/20 |
| H3 | Refiner/Ollama | 真环境 `OLLAMA_BASE_URL` 联调 30s 超时与熔断冷却 30s，验证 `refiner_worker` 异步落库 | 后端 B | 09/18 |
| H4 | NATS 集群 | `kubectl apply -f deploy/k8s/nats-init.yaml` 后 `nats stream info ASR_INPUT/ASR_OUTPUT` + `nats --jsz` pending<100 | DevOps | 09/15 |
| H5 | 文件提交 | `git add src/web_ui.py src/storage_worker.py src/refiner.py src/refiner_worker.py src/asr_worker.py src/gateway.py deploy/requirements.txt tests/test_stage2.py` | 后端 A | 09/10 |

### 12.4 排期调整

* **总体提前：** 阶段2 提前 27 天，W4 释放；P2（原 W5 10/13-10/19）可提前至 **09/12-09/20** 启动，P3 预研亦可并行。
* **W4 职责变更：** 原“P1 实现”改为“真机联调与吞吐压测窗口（H1-H4）”；若 H2 未达 20 msg/s，`aioboto3/motor` 回退至 `run_in_executor` 已就绪，不阻塞。
* **里程碑更新：** M2 标记“代码 95%，待真机吞吐关门”，M3 提前；甘特图 W4 标“P1 已完成”。
* **缓冲保留：** 缓冲 1 周仍保留至 10/27-11/02，应对联调回退。

### 12.5 下周（P2）立即动作

1. 提交阶段2 产物并打 tag `M2-p1-2026-09-09`，`kubectl rollout restart` 灰度 10% 观察 `asr.output.transcript` 轻量效果。
2. 启动 P2：3.1 Prometheus `/metrics`、3.2 `liveness/readiness`、3.3 `hpa.yaml`（HPA 2-5 副本）。
3. 预约 Jetson+NATS 真机完成 H2 压测，产出 `docs/perf_stage2_report.md`。

### 12.6 风险更新

* **降低：** `aioboto3/motor` 依赖风险已缓解（`HAS_AIOBOTO/HAS_MOTOR` 降级 + `run_in_executor` 兜底）；`webrtcvad` 体积风险不变。
* **新增：** `motor` 异步批量与同步 `pymongo` 混用可能导致重复写入 — 缓解：`HAS_MOTOR` 优先单路径，同步仅兜底，压测时监控 `transcriptions` 计数。
* **原风险保留：** `int8_float16` 真机 WER、`NATS Stream` 重建丢失风险延续，需 H4 前备份。

---

## 13. 阶段 3 评估与计划更新（2026-09-09）

### 13.1 评估结论（对标 W5 计划 7 人日）

* **进度：** 计划 W5(10/13-10/19) 7 人日，实际 09-09 已完成代码 96%（约 6.7 人日），提前 34 天。`pytest` 25 passed（`test_stage3` 7 + `test_stage2` 7 + `test_gateway_chunker` 8 + `benchmark` 3），`src/metrics.py`+`deploy/k8s/hpa.yaml`+`Grafana看板`+`perf_tuning_report` 均已就绪，未提交。
* **质量：** 代码级 P2 目标已达成（Prometheus 9指标+Gateway /metrics+Workers sidecar + probes + HPA + Grafana + 调优报告），剩余 4% 为集群真实压测：HPA `kubectl top` 与 10并发×5min `P95<1.2s` 需 Jetson+NATS 真环境。
* **准入判断：** 满足 M3 代码准出，可进入阶段 4 联调灰度；全链路压测作为 M3 关门项，不阻塞发布准备。

### 13.2 分项完成度

| 阶段 | 计划人日 | 完成度 | 已交付 | 待补 |
|------|----------|--------|--------|------|
| 3.1 Prometheus | 2 | ✅ 100% | `metrics.py` 9指标+降级+Gateway 3端点+Workers 3 sidecar | Grafana 数据源联调 |
| 3.2 健康检查 | 1.5 | ✅ 100% | `02-apps.yaml` liveness/readiness+resources+metrics端口 | K8s `kubectl describe pod` 验证重启 |
| 3.3 HPA | 1.5 | ✅ 100% | `hpa.yaml` gateway 2-5 + worker 1-3 双 HPA | `prometheus-adapter` pending 指标适配 |
| 3.4 背压协议 | 1 | ✅ 100% | `gateway_backpressure_total`+TokenBucket+WebUI 已打通 | 前端 pending>20 真机文案 |
| 3.5 压测调优 | 1 | ✅ 90% | `Grafana看板.json` 8面板+告警+`perf_tuning_report.md` | 10并发×5min 真机 `P95<1.2s` |

### 13.3 待补清单（合计 1 人日，M3 关门项，可与阶段 4 并行）

| # | 待补 | 动作 | 负责人 | 截止 |
|---|------|------|--------|------|
| I1 | HPA 集群验证 | `kubectl apply -f deploy/k8s/hpa.yaml` 后 `kubectl get hpa --watch` + `kubectl top pods` + 压测触发扩容 2→5 | DevOps | 09/18 |
| I2 | 全链路压测 | Jetson 真环境 10并发×5min，Grafana 截屏 `inference p95/s3 p95/queue depth`，更新 `perf_tuning_report` 附录 | 后端 B | 09/22 |
| I3 | Prometheus 联调 | 配置 `ServiceMonitor`/`PodMonitor` 抓取 `8000/metrics` 与 `8081-8083/metrics`，验证告警 `pending>100` | DevOps | 09/20 |
| I4 | 文件提交 | `git add src/metrics.py src/gateway.py src/asr_worker.py src/storage_worker.py src/refiner*.py deploy/k8s/02-apps.yaml deploy/k8s/hpa.yaml docs/Grafana看板.json docs/perf_tuning_report.md tests/test_stage3.py deploy/requirements.txt` | 后端 A | 09/10 |

### 13.4 排期调整

* **总体提前：** 阶段3 提前 34 天，W5 释放；阶段4（联调灰度文档）可提前至 **09/10-09/25** 启动，原缓冲 1 周保留至 10/27-11/02 应对压测回退。
* **W5 职责变更：** 原“可观测+弹性”改为“集群验证与压测窗口（I1-I3）”；若 I2 未达 P95<1.2s，`int8`/`max_concurrency` 再调优，缓冲兜底。
* **里程碑更新：** M3 标记“代码 96%，待集群关门”，M4 提前；甘特图 W5 标“P2 已完成”。
* **缓冲保留：** 缓冲 1 周不变。

### 13.5 下周（阶段 4）立即动作

1. 提交阶段3 产物并打 tag `M3-p2-2026-09-09`，`kubectl rollout restart` 后验证 `/metrics` 与 `/healthz`。
2. 启动阶段 4：4.1 集成回归 `tests/test_nats_core.py`、4.2 灰度 `USE_BINARY_PAYLOAD` 10%→100%、4.3 `运维手册`。
3. 预约集群完成 I2 压测，产出最终 `docs/perf_final_report.md`。

### 13.6 风险更新

* **降低：** 可观测缺失风险已解除（metrics+probes+HPA+Grafana 已闭环）。
* **新增：** HPA `pending` 自定义指标需 `prometheus-adapter`/`keda`，当前为 `Pods` 类型占位 — 缓解：先以 CPU 70% 为主，pending 仅参考，P2 后联调。
* **原风险保留：** `int8_float16` WER、NATS 重建丢失延续，需 I2 前备份。
